// citing a hole-tainted theorem inherits the hole: this file has no `hole` of
// its own, but rests on hole.b4m's zeroIsEven through restsOnHole.
import h <<< "hole.b4m"
sort Nat = h.Nat
const ZERO = h.ZERO
pred even = h.even
theorem restsOnHole = h.restsOnHole
theorem transitiveHole: even(ZERO)
proof
  @c |
    even(ZERO)
    [by cite restsOnHole]
qed
