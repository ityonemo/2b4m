// the SOURCE theory for define_alias_nested.bpa: a define other files alias.
sort Int
func mul(a: Int, b: Int) => Int
func sub(a: Int, b: Int) => Int
define divides(d, n) = exists k: Int; n = mul(d, k)
