# The Multiplicative Group of Complex Numbers (Exercises)

Exercises for Judson **§4.2 "The Multiplicative Group of Complex Numbers"**
(source: `vendor/aata/src/cyclic.xml`; © 1997–2026 Judson, GFDL 1.3), following
`.claude/rules/aata-guide.md`. The **modulus identities** (Exercise 19) are proved
from `std/complex-modulus.bpa`; the arithmetic/polar/DeMoivre computations are
**Skipped** (brute numeric) or **Deferred** on the trigonometry gap of §4.2.

The complex-arithmetic exercises (Exercises 15–18) — evaluating sums, products,
conjugates, powers of specific complex numbers, and converting between rectangular
and polar form — are **Skipped** as brute numeric computation (and 16–18 need the
`cis`/polar machinery that requires the absent trigonometry). The roots-of-unity
exercises (20, 21 — list and graph the 5th/6th roots, identify the primitive ones)
are **Deferred** on the same trigonometry gap (they are stated via
$\operatorname{cis}(2k\pi/n)$; see §4.2's deferral note).

We import the complex modulus, aliased to the book's notation.

```bpa
import complex <<< "std/complex.bpa"
import complex_modulus <<< "std/complex-modulus.bpa"
import real <<< "std/real.bpa"

sort C = complex.Complex
func mul = complex.mul
func conj = complex.conj
func abs = complex_modulus.abs
func normSq = complex_modulus.normSq
```

## Exercise 19 — modulus identities

**Prove each of the following.** (a) $|z| = |\overline{z}|$ — (b) $z\overline{z}
= |z|^2$ — (c) $z^{-1} = \overline{z}/|z|^2$ — (d) $|z + w| \le |z| + |w|$ — (e)
$|z - w| \ge \big||z| - |w|\big|$ — (f) $|zw| = |z|\,|w|$.

Parts (a), (b), (f) are proved from the modulus theory of
`std/complex-modulus.bpa` (which derives them from ℂ's projection algebra, no
trigonometry). Part (b) is stated with $|z|^2$ as the embedded real
$\mathrm{normSq}(z) = \mathrm{re}(z)^2 + \mathrm{im}(z)^2$.

**(a) $|\overline{z}| = |z|$** — conjugation preserves the modulus.

```bpa
theorem modulusOfConjugate: forall z: C; abs(conj(z)) = abs(z)
proof
  @conclusion |
    forall z: C; abs(conj(z)) = abs(z)
    [by theorem complex_modulus.absConj]
qed
```

**(b) $z\overline{z} = |z|^2$** — the product of $z$ with its conjugate is the
norm-square, embedded as a real.

```bpa
theorem conjugateProductIsNormSq: forall z: C; mul(z, conj(z)) = complex.fromReal(normSq(z))
proof
  @conclusion |
    forall z: C; mul(z, conj(z)) = complex.fromReal(normSq(z))
    [by theorem complex_modulus.zTimesConjIsNormSq]
qed
```

**(f) $|zw| = |z|\,|w|$** — the modulus is multiplicative.

```bpa
theorem modulusMultiplicative: forall z, w: C; abs(mul(z, w)) = real.mul(abs(z), abs(w))
proof
  @conclusion |
    forall z, w: C; abs(mul(z, w)) = real.mul(abs(z), abs(w))
    [by theorem complex_modulus.absMul]
qed
```

**(c), (d), (e) Deferred.** (c) $z^{-1} = \overline{z}/|z|^2$ needs the complex
reciprocal tied to the modulus (an equation over $\mathbb{C}^*$); reachable from
`zTimesConjIsNormSq` plus the field inverse. (d) the triangle inequality and (e)
the reverse triangle inequality need the Cauchy–Schwarz estimate
$\mathrm{re}(z\overline{w}) \le |z|\,|w|$ — an order argument on real parts beyond
the current modulus lemmas.
